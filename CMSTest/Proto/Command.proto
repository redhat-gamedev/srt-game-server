package command;

import "SecurityCommand.proto";


message Command
{
	enum CommandType
	{
		SECURITY = 0;
	}
	required CommandType type = 1 [default = SECURITY];
	optional SecurityCommand securityCommand = 3;
}
